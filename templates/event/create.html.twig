{% extends 'base.html.twig' %}
{% block title %}creation event{% endblock %}



{% block body %}
    <h2>Creation d'un event</h2>

    {{ form_start(formView) }}

    {{ form_widget(formView) }}
    
    {{ form_errors(formView) }}

         
        <button type="submit" class="btn btn-primary">
            <i class="fas fa-save"></i>
            Créér l'event
        </button>
            
    {{ form_end(formView) }}
{% endblock %}
